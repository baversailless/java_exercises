1)
- Создать интерфейс который описывает общее поведение инструментов обработки ингредиентов (ингредиент это строка), а именно метод process принимающий список/массив ингредиентов
- Реализация по умолчанию выводит список ингредиентов пришедших в метод
- Создать реализацию инструмента измельчителя, который измельчит каждый поступивший ингредиент (добавит к имени ингредиента “shredded”) и результат вывести в консольСоздать абстрактный класс который представляет собой инструмент (должен реализовать интерфейс инструмента) обрабатывающий ингредиенты с определенной температурой (состояние объектов такого типа), реализация метода process:
 вывод в консоль “списокИнгредиентов processed with temperature: значение температуры”
- Создать класс представляющий собой духовку (инструмент обрабатывающий с определенной температурой), у этого класса есть еще название модели духовки, дополнить реализацию метода process из абстрактного класса выводом модели духовки в консольПродемонстрировать работу измельчителя и духовки в методе main
2) Создать класс ингредиент (Ingredient), который имеет название и тип, тип это enum с значениями (RAW, BOILED, FRIED, SHREDDED)
   Из предыдущего домашнего задания использовать интерфейс инструмента (только используя вместо строк ингредиентов, новый класс) и реализовать измельчитель который меняет тип ингредиента на SHREDDED
   С использованием анонимного класса
   С использованием лямбды
   С использованием ссылки на метод
   Создать интерфейс SmartInstrument, у которого есть метод process Supplier<List<Ingredient> и объект типа Predicate<Ingredient>
   Используя лямбды реализовать поведение кастрюли которая варит только сырые или порезанные ингредиенты
   Реализовать поведение сковородки, которая жарит только порезанные ингредиенты, используя ссылку на метод



